# Converts probability (prevalence) to log-odds

`get_odds_from_prev` Converts probability (prevalence) to log-odds

## Usage

``` r
get_odds_from_prev(prev)
```

## Arguments

- prev:

  A probability or prevalence value. Must be between 0 and 1.
