# load_file

Load a file from within the inst/extdata folder of the anatembea
package. File extension must be one of .csv, .txt, or .rds.

## Usage

``` r
load_file(name)
```

## Arguments

- name:

  the name of a file within the inst/extdata folder.

## Details

`load_file` loads package file
