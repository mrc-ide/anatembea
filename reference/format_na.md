# Change missing data to 0s so dbinom function returns 0 (instead of NA)

`format_na` Change missing data to 0s so dbinom function returns 0
(instead of NA)

## Usage

``` r
format_na(df)
```

## Arguments

- df:

  Data input from user Default = NULL
